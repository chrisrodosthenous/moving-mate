/**
 * @deprecated Use {@link DialogService} from `./dialog.service` instead. Kept
 * for backward compatibility with `inject(ConfirmDialogService)`.
 */
export { DialogService, DialogService as ConfirmDialogService } from './dialog.service';
